interface:
  display_name: "Pulse"
  short_description: "Repo-specific patterns and workflows for pulse"
  default_prompt: "Use the pulse repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true